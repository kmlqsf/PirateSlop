# PirateSlop — описание проекта и общие правила

Пушки, 2026-09-08: отдача/попадания раскачивают корабль; палубные пушки толкаются телом и откатываются, оставаясь на палубе. Полёт ядра использует swept-проверку столкновений и движение корабля. ProtocolVersion=24. Подробности — последний раздел combat-balance.md; игровая приёмка не выполнялась.

Фрегат, 2026-09-08: NetworkShip заменён новым трёхмачтовым PirateFrigate. Корпус 46×13 м, трюм с наклонной лестницей, два подъёма на мостик, воронье гнездо и открытые орудийные секции. Обновлены коллайдеры, штурвал, паруса, лестницы, спавн и габариты CanSail. ProtocolVersion=22. Подробности — frigate.md. Игровая и сетевая приёмка не выполнялись.

Инвентарь, 2026-09-08: сундуки открывают окно слотов с подбором кликом; добавлены иконки всех пяти типов предметов и хранение ядер (стек 20, зарядка через E). ProtocolVersion=20. Подробности — в начале procedural-world.md; игровая приёмка не выполнялась.

Лут, 2026-09-08: добавлены процедурные сетевые сундуки и общий каталог DefaultLoot (пока корабельная пушка), открытие и подбор через E. ProtocolVersion=19. Подробности и ручная приёмка — в начале procedural-world.md. Игровая проверка не выполнялась.

Краш запуска, 2026-09-08: три дампа 192219322, 192437886 и 211309909 расшифрованы локальными PDB Unity 6000.6.0f1. Во всех EXCEPTION_ACCESS_VIOLATION (0xc0000005), UnityPlayer+0x2135a13: FilterInstancesAndCollectScriptingObjectsJobData::ExecuteJob_NoScriptingArray+0xa3. В URP GPU Resident Drawer вызывает ObjectDispatcher.GetTypeChangesAndClear с noScriptingArray=true. Отключён GPU Resident Drawer в Assets/Settings/PC_RPAsset.asset (m_GPUResidentDrawerMode=0), Mobile уже отключён. Это адресный обход механизма падения; точный внутренний дефект движка не установлен. Graphics Jobs был отключён и раньше, этого было недостаточно. Новая настройка требует пересборки; удаление папок не устраняет этот механизм. Повторные запуски новой сборки ещё не проверены. Не включать GPU Resident Drawer обратно без отдельной проверки стабильности. Расшифрованный стек сохранён локально в Temp/startup-crash-symbols.txt.

Острова снабжения, 2026-09-07: добавлена сборка внутреннего наполнения по seed через SupplyIslandComposition. Варианты боковых стен/навесов, дополнительные укрытия и новая расстановка растительности/камней; пирсы и точки припасов сохранены. ProtocolVersion=18, CatalogRevision=6. LOD моделей отложен. Игровая приёмка не выполнялась; подробности в начале procedural-world.md.

Процедурная карта, 2026-09-07: DefaultWorld переведён на 16 стартовых секторов, 4 спорные локации и 8 препятствий; сохранены 32 слота кораблей, общих экипажей пока нет. Добавлены морские маршруты в JSON и три визуальных LOD рельефа с полной сеткой коллизии. WorldLayout=5, ProtocolVersion=17, CatalogRevision=5. Подробности и ограничения: procedural-world.md. Игровая приёмка пользователем ещё нужна.

Лестницы: на NetworkShip добавлены внешняя лестница правого борта (середина корабля) и лестница к площадке главной мачты. Подъём начинается движением в лестницу без E; направление движения/взгляда задаёт подъём и спуск, Space отталкивает, A/D позволяют сойти. Камера не блокируется. Старый переход E из воды на палубу удалён. Состояние лазания синхронизируется, протокол 10. Игровую приёмку не запускали.

Актуальный запуск: NetworkMenu → NetworkOcean. SampleScene удалена; упоминания ниже относятся к истории проекта. Play Mode по умолчанию запускает NetworkMenu, даже при редактировании NetworkOcean. Настройка персонажа и корабля выполняется на сетевых префабах; исходной локальной сцены больше нет. Процедурная генерация и остальные текущие изменения передаются вместе с этим переходом.

Согласованный план урона, однозарядного пистолета, ножа и возрождения: [combat-balance.md](combat-balance.md). Это целевой баланс для реализации, не подтверждение готовности механик; его решения уточняют прежние открытые вопросы о смерти и завершении дуэли ниже.

Обновлено: 2026-09-06. Репозиторий: https://github.com/kmlqsf/PirateSlop.
Все пути ниже отсчитываются от корня репозитория. Имя локальной папки может отличаться.

## Плавание и эффекты, 2026-09-07

OceanSurface сохранён также в NetworkOcean. Плавание: WASD, Shift — быстрее, Space — вверх, Ctrl/C — вниз, E — выход на доступную палубу. Добавлены плавучесть, воздух на 25 секунд, серверный урон при удушье, подводный туман и индикатор воздуха, процедурные клипы плавания. Оружие и перенос ядер отключены во время плавания. Выстрелы пистолета/пушки получили вспышку, дым, искры, свет, попадания — пыль и частицы, вода — всплески. Протокол 8; оба игрока должны обновиться. Игровая и визуальная проверка не выполнялась.

## Игра и MVP

Процедурная карта V1, 2026-09-07: в SampleScene и NetworkOcean подключён ProceduralWorld. Каталог из шести форм островов/рифов/скал, seed, точки расширения и JSON-карты. Сервер ожидает совпадение карты перед спавном. Параметры и ограничения: [procedural-world.md](procedural-world.md). Протокол 9; игровая и сетевая приёмка не выполнялась.

Цель текущего MVP — сетевая дуэль на пиратских кораблях: два игрока с разных ПК подключаются к одной сессии, каждый управляет своим кораблём и может стрелять по кораблю другого игрока. Долгосрочная идея battle royale может обсуждаться позже, но сейчас не является требованием MVP. Игровой ориентир по ощущениям совместного управления и морского боя — Sea of Thieves. How to Fish используется только как визуальный референс для создания моделей и ничего больше: не копировать материалы, механики, UI, настроение страницы магазина или игровые правила. Подтверждённый визуальный референс: [How to Fish](https://store.steampowered.com/app/4001890/How_to_Fish/). Перед художественной работой изучить изображения игры по этой ссылке.

MVP: два корабля и два реальных игрока. Игроки с разных ПК подключаются через интернет к одной сессии; каждый игрок управляет своим кораблём и может стрелять по кораблю другого игрока. Поддержка только localhost или локальной сети не удовлетворяет MVP:

- два корабля — по одному управляемому кораблю на игрока;
- штурвал — управление своим кораблём;
- пушки — стрельба по кораблю другого игрока;
- урон корабля — наблюдаемое состояние, одинаковое для обоих игроков.

Боты, battle royale-цикл, сужение зоны, матчмейкинг, прогрессия, экономика и большой онлайн не входят в согласованный MVP. Не добавлять их в текущую задачу без отдельного запроса. Победу, затопление, возрождение и правила окончания тестовой сессии уточнять при реализации соответствующего этапа.

## Что есть сейчас

Проверено по файлам проекта, без запуска игры:

- Unity `6000.6.0f1`; URP `17.6.0` установлен, в уровнях качества PC и Mobile назначены pipeline assets.
- Сцена `Assets/Scenes/SampleScene.unity`.
- Код в `Assets/Scripts`: движение игрока и корабля, взаимодействия, штурвал и парус, HUD и автоматическая настройка объектов.
- В `Packages/manifest.json` есть Input System `1.20.0` и Unity MCP от CoplayDev. Соединение Unity MCP через локальный HTTP настроено и проверено только на ПК первого разработчика; у второго разработчика оно может быть не настроено, повторение описано в unity.md.
- Сетевой стек — FishNet 4.7.2 + Tugboat; есть код сессии, сетевые префабы и Windows-сборка. Последующие правки движения, столкновений и чужого штурвала описаны в multiplayer-plan.md. Их двухклиентная приёмка ещё не подтверждена; пушки и урон остаются отдельным этапом.
- Blender `5.2.1 LTS` и управление через локальный MCP bridge `127.0.0.1:9876` настроены и проверены только на ПК первого разработчика; у второго разработчика они могут быть не настроены, повторение описано в blender.md.

Наличие скрипта не означает, что механика подключена к сцене или работает. Перед задачей перепроверять актуальные файлы: этот раздел является снимком состояния.

## Работа ИИ

Пользователь описывает результат, ИИ выполняет доступную работу целиком: код, объекты, компоненты, ссылки, модели, импорт, префабы и сохранение. Ожидаемая передача пользователю — открыть указанную сцену, запустить и проверить, без ручного навешивания скриптов.

Обычные действия внутри задачи выполнять самостоятельно. Уточнять только существенную неопределённость, влияющую на результат, или недостающее разрешение. Не считать широкое разрешение на работу разрешением на коммит и push: для них действует отдельное правило ниже.

Перед изменениями прочитать нужные инструкции и существующую реализацию. Сохранять чужие незавершённые изменения. Не переделывать несвязанные системы. Если инструмент или редактор недоступен, выполнить независимую часть и точно назвать оставшийся шаг; не заявлять, что сцена настроена или проверена без подтверждения.

Документация и ответы — по-русски. Код, имена файлов и объектов — по-английски. Обновлять эти документы при изменении фактической архитектуры и согласованных решений. Не записывать предложения как принятые решения.

## Совместная работа и Git

Два разработчика работают над всеми частями проекта по очереди и передают результат через GitHub. Рабочую ветку определять по репозиторию и текущей договорённости, не предполагать её имя.

1. В начале проверить корень репозитория, `git status`, ветку и remote. При доступной сети получить состояние remote через fetch.
2. Если рабочая копия чистая, обновить согласованную ветку через `git pull --ff-only`. Если есть локальные изменения или расхождение истории, сохранить их и разобраться; не применять автоматический reset, clean, stash или force push.
3. На время передачи не редактировать одни и те же сцены, префабы и бинарные модели на двух ПК. Git сам по себе не обеспечивает такую блокировку.
4. Перед публикацией сохранить редакторы, проверить diff, перечислить изменённое, выполненные проверки и оставшиеся ограничения. Предложить конкретный коммит и целевую ветку.
5. **Создавать коммиты и выполнять push только после явного подтверждения в чате.** Одно подтверждение может разрешать оба действия для описанного набора изменений. Подтверждение только коммита не разрешает push. Не считать подтверждение бессрочным для будущих задач.
6. После разрешения включить только относящиеся к задаче файлы, выполнить коммит и push, сообщить хеш и результат отправки. При конфликте с новым push друга не перезаписывать remote.

Сохранять `.meta` вместе с Unity-ассетами; переносить ассеты с сохранением GUID. Не коммитить `Library`, `Temp`, `Logs`, `UserSettings`, локальные токены и настройки соединений. Соблюдать имеющиеся `.gitignore` и `.gitattributes`: FBX, BLEND и многие другие бинарные ассеты уже используют Git LFS. На обоих ПК проверять, что скачаны реальные LFS-файлы.

## Приёмка

Игровое поведение пока проверяют разработчики вручную. ИИ выполняет доступные проверки компиляции, импорта и ссылок, но не подменяет ими проверку геймплея. Для каждой задачи дать короткие шаги проверки и ожидаемый результат; ясно отделить выполненное от непроверенного. Не вводить обязательный большой набор автотестов без необходимости.

Критерии MVP: два клиента с разных ПК через интернет участвуют в одной сессии; каждый игрок находится на своём корабле, управляет им через штурвал и стреляет из пушек; попадания и урон видны обоим игрокам одинаково; выход игрока и освобождение управления корректно синхронизированы.

## Решения, которые предстоит принять

- Целевая платформа и минимальная производительность.
- Реализация уже обязательной игры через интернет: транспорт, хостинг, подключение через NAT и сетевой стек.
- Детали повреждений, поражения, ремонта и правил окончания дуэли.
- Повторная настройка и проверка управления Unity и Blender с ИИ на втором ПК.

Уточнять эти пункты перед зависимой задачей; они не блокируют независимую работу.

## Текущий сетевой этап (2026-09-06)
Согласован Windows, FishNet + Tugboat, до 32 участников, каждому свой корабль. Сейчас сетевое движение и подключение перед боёвкой. План и фактический статус: [multiplayer-plan.md](multiplayer-plan.md). Прежнее указание о невыбранном стеке относится к исходному состоянию.

Уточнение пользователя от 2026-09-06: после перехода на другой корабль должна быть возможность занять его свободный штурвал. Ограничение «только свой корабль» выше описывает первоначальный этап и больше не является требованием. Состояние передачи реализации и известные ограничения приведены в конце multiplayer-plan.md; прочитать их перед продолжением сетевой работы.

## Анимации персонажа (2026-09-06)

Подключены семь анимаций к локальному и сетевому игроку; F1 переключает вид камеры для осмотра тела. Сетевой протокол повышен до 2 из-за дополнительных полей состояния анимации. Windows-сборка выполнена, базовый обмен между двумя процессами на localhost проверен. Перечень сделанного, оставшихся клипов и ручной приёмки: [animation-review.md](animation-review.md).

## Боёвка и контакт кораблей — актуализация 2026-09-06

Добавлены серверный урон и синхронизированные полоски здоровья: пират 100 HP, корабль 1200 HP. Пистолет и нож повреждают персонажа, выстреленное ядро — корабль. При смерти пират возрождается через 5 с с полным HP, корабль исчезает без анимации затопления. Фактические параметры, наведение пули и оставшиеся ограничения: combat-balance.md, раздел «Реализовано на 2026-09-06». Круговое раздвижение кораблей заменено проверкой пересечений коллайдеров; подробности в конце multiplayer-plan.md. Эти сведения заменяют раннее указание, что урон остаётся отдельным этапом.

Инвентарь и установка пушек: добавлены CannonballCrate, подбираемая DisassembledCannon, шесть слотов и одноразовая установка с предпросмотром. Сетевой протокол 3; детали и ограничения — последний раздел combat-balance.md. Игровая проверка этих изменений ещё не выполнена.

Прямое управление, 2026-09-07: штурвал и наклон ствола управляются перетаскиванием без блокировки движения; паруса — колесом возле мачты с UI скорости. Установка пушек поддерживает любые поверхности и ориентации. Протокол 4; подробности — последний раздел combat-balance.md.

Звук, 2026-09-07: добавлены CC0-записи оружия, шагов, взаимодействий, урона, корабля, ветра и моря. Назначения и громкости — Assets/Resources/GameAudioBank.asset; источники — Assets/Audio/CREDITS.md. Компоненты сохранены в сцене и игровых префабах. Протокол 5 из-за сетевых событий звука корабля. Прослушивание и игровую проверку выполняет пользователь.

Вода, 2026-09-07: OceanSurface и PirateSlop/Ocean заменяют плоскую воду в SampleScene. Четыре общих CPU/GPU волны, мелкая рябь, отражение окружения, преломление/цвет по глубине, пена и процедурный след кораблей. Сетка 8 км следует за камерой; плотность выше рядом с игроком. ShipController усредняет четыре точки поверхности для вертикального движения, дифферента и крена; это кинематическая плавучесть, не гидродинамическая симуляция. Снимки передают наклоны и фазу волн; протокол 6 требует обновления обоих игроков. Ядра определяют контакт с волнами. Настройки: OceanWater.WaveScale, ShipController.buoyancyResponse/floatLength/floatWidth, Assets/Materials/Ocean.mat. Плавание, подводный рендер и физическое затопление не добавлены. Внешний вид, движение по качающейся палубе и сетевую приёмку проверяет пользователь.


Остров-прототип, 2026-09-07: в DefaultWorld включён supply_island с модульной хижиной, причалом, растительностью и маркерами лута. 12 моделей созданы через Blender MCP, префабы и URP-материалы сохранены через Unity MCP. WorldLayout 2, протокол 14. Детали и ручная приёмка: procedural-world.md. Play Mode и сетевая проверка не выполнялись.


Острова снабжения V2, 2026-09-07: три варианта с 1–3 пирсами, радиус 90 м, холмы, скалистые берега и более плотное окружение. Набор Blender расширен до 22 моделей с детализацией по конструктивному ориентиру ShipCannon.blend. WorldLayout 3, протокол 15. Детали в procedural-world.md; игровая и визуальная приёмка остаётся пользователю.

Расширение локаций, 2026-09-07: пять тем по два варианта (бухты, руины, кораблекрушения, каменные и рифовые проходы), 21 новый Blender-модуль. Каталог подключён к DefaultWorld; WorldLayout 4, протокол 16. Подробности в procedural-world.md. Игровая проверка остаётся пользователю.

## Версия игры на экране

В нижнем правом углу меню и игры автоматически отображается `v<major>.<minor>.<число коммитов> · <короткий hash>`. Например, v0.1.36 · c481b663. Major/minor берутся из PlayerSettings.bundleVersion; номер ревизии и hash — из текущего HEAD. Это версия отображения игры, независимая от сетевого ProtocolVersion; bundleVersion автоматически не переписывается.

При локальных незакоммиченных изменениях добавляется +dev. При shallow clone добавляется (shallow): число коммитов в таком клоне неполное, точный идентификатор — hash. При недоступном Git показывается unknown, старый штамп не используется как актуальный.

Штамп пересчитывается при загрузке Editor-скриптов, перед Play Mode и перед сборкой; вручную — PirateSlop → Refresh Game Version. Во время уже запущенной игры версия фиксирована. Сборка содержит свой штамп и не требует Git на компьютере игрока. Для одинаковых номеров ревизий использовать полный клон репозитория. Amend может сохранить число коммитов, но изменит hash.

GameVersionOverlay создаётся автоматически, сохраняется между сценами. GeneratedGameVersion.txt и его .meta игнорируются Git, чтобы обновление штампа само не помечало рабочую копию изменённой. Коммит-хуки и ручное повышение patch не требуются. Компиляция и создание штампа проверяются отдельно от отображения в Play Mode; визуальную приёмку выполняет пользователь.
## Краш Windows-сборки, 2026-09-07

В Player.log подтверждён нативный краш UnityPlayer при запуске Windows-сборки; стек содержит только UnityMain и системные вызовы, поэтому точная причина пока не установлена. Исключения GameVersionOverlay в журнале не обнаружено. Перед крашем есть предупреждения URP о вырезанных post-processing shaders; сами по себе они не доказывают причину падения.

Последняя сборка повторно использовала данные игрока из кэша. В PirateSlop → Multiplayer → Build Windows добавлен BuildOptions.CleanBuildCache, чтобы исключить устаревшие данные после изменений ассетов. Это первый шаг устранения, не подтверждённое исправление краша. Новую сборку и запуск выполняет пользователь; при повторном падении изучить свежий Player.log и crash.dmp. GPU Resident Drawer и графические настройки на этом шаге не менялись.
